# dsh-novel-solo: dev patch overlay for the web profile.
#
# Usage:
#   dsh web --patch ./cordis.yml
#
# Inserts the plugin into the web composition so the Settings → Plugins
# subagent-count card is available during development.
- insert:
    - id: dsh-novel-solo
      name: 'dsh-novel-solo'